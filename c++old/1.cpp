#include <iostream>
#include <cmath>
using namespace std;

int main(){
	int a;
	cin >> a;
	cout << "P: " << a * 4 << endl; 
	cout << "S: " << pow(a, 2) << endl;
	cout << "d: " << sqrt(pow(a, 2) + pow(a, 2));
}